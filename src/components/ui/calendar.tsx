"use client"

import * as React from "react"
// The 'react-day-picker' dependency has been removed to resolve a build issue.
// The component is not currently in use.

export type CalendarProps = {}

function Calendar({}: CalendarProps) {
  return null
}
Calendar.displayName = "Calendar"

export { Calendar }
